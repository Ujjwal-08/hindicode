// 03_functions.hindi.js - Functions, closures, arrow functions

// ── Basic function ──
कार्य स्वागत(नाम) {
    लौटाओ `नमस्ते, ${नाम}!`;
}

दिखाओ(स्वागत("अर्जुन"));

// ── Arrow function equivalents (using native arrow syntax) ──
स्थिर वर्ग_फलन = (x) => x * x;
दिखाओ("वर्ग(5):", वर्ग_फलन(5));

// ── Recursive function ──
कार्य क्रमगुणित(n) {
    अगर (n <= 1) लौटाओ 1;
    लौटाओ n * क्रमगुणित(n - 1);
}

दिखाओ("5! =", क्रमगुणित(5));

// ── Closure ──
कार्य काउंटर_बनाओ() {
    नया संख्या = 0;
    लौटाओ कार्य() {
        संख्या++;
        लौटाओ संख्या;
    };
}

स्थिर गिनो = काउंटर_बनाओ();
दिखाओ(गिनो(), गिनो(), गिनो()); // 1 2 3

// ── Default parameters ──
कार्य परिचय(नाम = "अतिथि", आयु = 0) {
    दिखाओ(`नाम: ${नाम}, आयु: ${आयु}`);
}

परिचय("राहुल", 22);
परिचय();
