// 04_arrays.hindi.js - Array methods in Hindi

स्थिर फल = ["आम", "केला", "सेब", "अंगूर"];

// ── लंबाई ──
दिखाओ("फल की संख्या:", फल.लंबाई);

// ── पुश / पॉप ──
फल.पुश("संतरा");
दिखाओ("पुश के बाद:", फल);

नया निकला = फल.पॉप();
दिखाओ("पॉप किया:", निकला);

// ── मानचित्र ──
स्थिर बड़े_अक्षरों_में = फल.मानचित्र(f => f.toUpperCase());
दिखाओ("बड़े अक्षरों में:", बड़े_अक्षरों_में);

// ── फ़िल्टर ──
स्थिर छोटे_फल = फल.फ़िल्टर(f => f.लंबाई <= 3);
दिखाओ("छोटे फल (3 अक्षर):", छोटे_फल);

// ── कमकरो (reduce) ──
स्थिर अंक = [10, 20, 30, 40];
स्थिर योग = अंक.कमकरो((कुल, मान) => कुल + मान, 0);
दिखाओ("योग:", योग);

// ── शामिल / इंडेक्स ──
दिखाओ("'आम' शामिल?", फल.शामिल("आम"));
दिखाओ("'केला' कहाँ है:", फल.इंडेक्स("केला"));

// ── हरएक ──
दिखाओ("सभी फल:");
फल.हरएक(f => दिखाओ(" -", f));

// ── क्रमित / उलटाओ ──
नया संख्याएँ = [5, 2, 8, 1, 9];
दिखाओ("क्रमित:", संख्याएँ.क्रमित((a, b) => a - b));
दिखाओ("उलटा:", [...संख्याएँ].उलटाओ());

// ── ढूँढो ──
स्थिर पहला_बड़ा = अंक.ढूँढो(n => n > 25);
दिखाओ("पहला 25 से बड़ा:", पहला_बड़ा);
