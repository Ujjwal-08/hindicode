// 06_async.hindi.js - Async/Await and Promises

// ── Basic Promise ──
स्थिर वादा = नया बनाओ प्रॉमिस((हल, अस्वीकार) => {
    नया सफल = सच;
    अगर (सफल) {
        हल("काम पूरा हुआ! ✅");
    } वरना {
        अस्वीकार("कुछ गलत हुआ ❌");
    }
});

वादा
    .फिर(परिणाम => दिखाओ("वादा:", परिणाम))
    .पकड़(गड़बड़ी => गलती("गड़बड़ी:", गड़बड़ी));

// ── Async function ──
असिंक कार्य डेटा_लाओ() {
    कोशिश {
        स्थिर परिणाम = इंतज़ार नया बनाओ प्रॉमिस(हल => {
            समय_बाद(() => हल({ नाम: "रोहन", अंक: 95 }), 100);
        });
        दिखाओ("डेटा प्राप्त:", JSON.stringify(परिणाम));
        लौटाओ परिणाम;
    } पकड़ो (त्रुटि) {
        गलती("त्रुटि:", त्रुटि.message);
    }
}

डेटा_लाओ().फिर(() => दिखाओ("असिंक कार्य पूरा!"));

// ── Promise.all ──
स्थिर प्रॉमिस1 = प्रॉमिस.हल(10);
स्थिर प्रॉमिस2 = प्रॉमिस.हल(20);
स्थिर प्रॉमिस3 = प्रॉमिस.हल(30);

प्रॉमिस.सभी([प्रॉमिस1, प्रॉमिस2, प्रॉमिस3])
    .फिर(परिणाम => दिखाओ("सभी परिणाम:", परिणाम));
