// 02_control_flow.hindi.js - if/else, loops, switch

// ── if / else ──
नया अंक = 75;

अगर (अंक >= 90) {
    दिखाओ("ग्रेड: A");
} नहीं तो अगर (अंक >= 75) {
    दिखाओ("ग्रेड: B");
} वरना {
    दिखाओ("ग्रेड: C");
}

// ── for loop ──
दिखाओ("1 से 5 तक:");
केलिए (नया i = 1; i <= 5; i++) {
    दिखाओ(i);
}

// ── while loop ──
नया गिनती = 3;
जबतक (गिनती > 0) {
    दिखाओ("उलटी गिनती:", गिनती);
    गिनती--;
}

// ── switch / case / break ──
नया रंग = "नीला";
स्विच (रंग) {
    मामला "लाल":
        दिखाओ("रंग: लाल 🔴");
        रोकें;
    मामला "नीला":
        दिखाओ("रंग: नीला 🔵");
        रोकें;
    default:
        दिखाओ("अज्ञात रंग");
}

// ── do/while + continue ──
नया x = 0;
करो {
    x++;
    अगर (x === 3) जारी;
    दिखाओ("do-while x:", x);
} जबतक (x < 5);
